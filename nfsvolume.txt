1) Create a nfs server 
    1  hostname nfsserver 
    2  sudoapt update 
    3  sudo apt install nfs-server -y 
    4   sudo mkdir /datadir
    5  sudo chmod 777 /datadir
    6  sudo vim /etc/exports  add the following entry 
/datadir *(rw,sync) --- save the file 

    7  sudo exportfs -r 
    8  sudo exportfs
    9  sudo ip a
   10  history 

2) install nfs-common on worker nodes 
login into worker node 
sudo apt install nfs-common -y 


3) create a pod defination with nfs volume 
apiVersion: v1
kind: Pod


metadata:
  name: pod1


spec:
  containers:
    - name: cont1
      image: nginx
      volumeMounts:
        - name: vol1
          mountPath: /abc
  volumes:
    - name: vol1
      nfs:
        server: 172.31.14.235
        path: /datadir

4) Deploy the pod and check the status 
kubectl apply -f pod.yml  
   33  kubectl get pods 
   34  kubectl exec -it pod1 /bin/bash
